# ===== OS / Editor =====
.DS_Store
.idea/
.vscode/

# ===== Secrets & environment (keep *.example tracked) =====
.env
.env.*
!.env.example
.env.bak
.env.backup

# ===== Python =====
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
ENV/
*.egg
*.egg-info/
.pytest_cache/
.mypy_cache/

# ===== Node / Frontend build output =====
node_modules/
dist/
dist-ssr/
*.local

# ===== Build artifacts & archives =====
build/
*.zip
*.tar.gz
*.tgz

# ===== Terraform (state & tfvars hold secrets in plaintext — never commit) =====
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# ===== Deployment lives ONLY on the pilot_deployment branch — never commit it on this branch =====
deploy/

# ===== Site Agent (local FL/QC agent) =====
# Local token store + dev-only per-agent config dirs that held test tokens (never commit tokens)
.agentconfig*
**/.cfg_creator/
**/.cfg_invited/
**/.agentconfig_*/
# Generated collaborator kit
collabstudy-agent-kit.zip
# Agent local working data (per-site raw genotype CSVs — never leave the site)
web_application/SiteAgent/raw-data/

# ===== Logs & temp =====
*.log
*.tmp

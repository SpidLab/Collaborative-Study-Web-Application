# QC Worker Dependencies
# Web Framework (for warm pod HTTP server)
flask>=2.0.0

# Data Processing
pandas>=1.3.0
numpy>=1.20.0
scipy>=1.7.0

# Machine Learning (for PCA)
scikit-learn>=1.0.0

# MongoDB with SSL support
pymongo[srv,tls]>=4.0.0
certifi>=2020.12.5

# HTTP Client (for callbacks)
requests>=2.25.0

# QC Controller Dependencies

# Web Framework
flask>=2.0.0
flask-cors>=3.0.0

# Data Processing
pandas>=1.3.0
numpy>=1.20.0
scipy>=1.7.0

# Machine Learning (for PCA)
scikit-learn>=1.0.0

# MongoDB with SSL support
pymongo[srv,tls]>=4.0.0
certifi>=2020.12.5

# HTTP Client (for health checks)
requests>=2.25.0

# Standard library modules used (no installation required):
# - argparse (command line interface)
# - logging (progress tracking)
# - sys (system operations)
# - os (file operations)
# - datetime (timestamp generation)
# - pathlib (path handling)
# - io (string IO)
# - pickle (model serialization)
# - traceback (error handling)
# - urllib.parse (URL encoding)
